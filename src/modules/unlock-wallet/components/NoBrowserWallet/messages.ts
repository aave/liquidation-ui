import { defineMessages } from 'react-intl';

export default defineMessages({
  title: 'No browser wallet detected',
  description:
    'Please install a browser wallet like: {metaMask}, {dapper}, {trustWallet}. You can still connect to Aave through other wallet options available to you.',
});
